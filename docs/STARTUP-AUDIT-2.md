# Аудит стартового пути — итерация 2 (20 анализов)

Цель: найти ошибки ранних этапов, из-за которых проект не стартует «с начала»
(чистый клон → `.env` → установщик → самопроверка → первый тик → Telegram → гипотеза).

Метод: исполнение реальных команд на свежесозданных копиях проекта + статический
обход установщиков + e2e-установка с готовым `.env` и с ручным вводом.

## Найденные и исправленные дефекты

| # | Анализ | Дефект | Статус |
|---|--------|--------|--------|
| 1 | `install.sh` ручной ввод (ask) | **КРИТИЧНО**: `ask()` печатал промпт в stdout, захватываемый command substitution — токен записывался как `"TELEGRAM_BOT_TOKEN: <ввод>"`. Интерактивная установка без готового `.env` никогда не работала | 🔧 промпт → stderr; проверено: токен `123:abc` пишется чисто |
| 2 | `install.sh` без stdin / `--non-interactive` | `ask_req` зацикливался при закрытом stdin | 🔧 флаг `--non-interactive`, выход с понятной ошибкой; проверено: EXIT=1 без зависания |
| 3 | `install.ps1 -NonInteractive` без токена | `Ask-Required` → бесконечный `Read-Host` при закрытом stdin | 🔧 `[Console]::IsInputRedirected` → понятная ошибка |
| 4 | `install.ps1 -Full` со сменой корня | `.env` старого каталога перетекал в новый таргет | 🔧 повторное чтение `.env` целевого каталога после шага 2 |
| 5 | `setup.ps1` | clone падал на плейсхолдере `<ваш-логин>` без объяснения | 🔧 явная ошибка «владелец не настроил $Repo» |

## Проверено — ошибок нет

| # | Анализ | Результат |
|---|--------|-----------|
| 6 | e2e `install.sh` + готовый `.env` | подхват без вопросов, лимиты 20/20/12 реально применяются |
| 7 | `config.yaml` после установки | все `<<INSTALLER>>` заменены, парсинг дефолтов корректен |
| 8 | `cron/*.json` × 5 | валидный JSON, delivery-плейсхолдеры подставляются |
| 9 | `distribution.yaml` | читается; `homepage: <you>` — плейсхолдер владельца (minor) |
| 10 | skills: frontmatter 23/23 + команды | все `name/description` валидны; все вызываемые команды существуют в `rg.py`/`tools/` |
| 11 | `selfcheck.py all` (клон и профиль) | корректные ❌/⚠️, exit 1 без traceback |
| 12 | `rg.py boot / status / doctor / panel / digest / weekly / recalib / calib` | exit 0 на свежей базе |
| 13 | `rg.py tick` (0 гипотез, нет GPU) | `[idle]` — аккуратный простой |
| 14 | `board.py sync` без hermes | мягкое «канбан пропущен» |
| 15 | `tg.py test` с chat=0 и фейк-токеном | мягкий отказ, не краш |
| 16 | `rg.py digest --send` без чата | понятная ошибка, не traceback |
| 17 | цикл add → check (гейт) → launch (блок) → verdict → kill | полный цикл работает; урок пишется в `memory/killed.md` |
| 18 | `running / preempt / signals / next / queue` | аккуратные ответы на пустой базе |
| 19 | `bottom run / stats / regions / evidence` | регионы из `MISSION.md` строятся, exit 0 |
| 20 | miniapp: GET `/api/state` (200), POST `/api/action` | JSON-ответ без краша |

## Регрессия после правок

- `python -m unittest discover -s tests` → **176/176 OK**
- `python tools/audit.py run --no-coverage` → **80/80, 0 FAIL**
- `sh -n install.sh` → синтаксис OK

## Что осталось за скобками (не блокеры старта)

- `hermes` CLI (gateway/cron) не тестируется в песочнице — проверяется на машине владельца.
- Реальная отправка в Telegram требует настоящего токена.
- GPU-прогоны требуют RTX 5090 + Ollama + скачанную модель (установщик проверяет и подсказывает).
